# SpringBoot 应用 Docker 容器化部署说明

## 项目配置已完成

### 1. 已修改的配置文件

#### application.yml
- 服务端口已改为: `10011`

#### pom.xml
- 已添加 `<finalName>dogFooding</finalName>` 配置
- Jar包名称固定为: `dogFooding.jar`

### 2. 已创建的文件

#### Dockerfile
```dockerfile
FROM openjdk:8-jre-alpine
WORKDIR /app
RUN ln -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime
RUN echo 'Asia/Shanghai' > /etc/timezone
COPY dogFooding.jar /app/
EXPOSE 10011
ENV JVM_OPTS="-Xms256m -Xmx512m"
ENTRYPOINT ["sh", "-c", "java $JVM_OPTS -jar dogFooding.jar"]
```

#### deploy.ps1 (PowerShell部署脚本)
- 自动完成: 本地文件检查 → 服务器连接测试 → 环境检测 → 文件上传 → Docker构建 → 容器部署 → 服务验证
- 自动处理: 端口占用检测、旧容器/镜像清理

### 3. 部署执行步骤

#### 前置条件
1. 确保本地已配置SSH密钥可无密码登录服务器
2. 确保本地已安装Maven和Git Bash(或使用PowerShell)
3. 服务器已安装Docker

#### 执行部署
```powershell
# 打开PowerShell，进入项目目录
cd d:\workspace\traeTest\trae0322\trae032203\trae0322_dogFooding

# 执行部署脚本
.\deploy.ps1
```

### 4. 服务器信息
- IP: `49.235.161.106`
- SSH端口: `22`
- 目标目录: `/opt/apps/memo-app`
- 服务访问地址: `http://49.235.161.106:10011`

### 5. 部署脚本功能说明

| 步骤 | 功能 |
|------|------|
| 1 | 检查本地jar包和Dockerfile是否存在 |
| 2 | 测试服务器SSH连接 |
| 3 | 检测服务器Docker环境 |
| 4 | 检查服务器现有配置(是否已有Dockerfile) |
| 5 | 上传jar包和Dockerfile到服务器 |
| 6 | 远程构建Docker镜像 |
| 7 | 检测端口占用，清理旧容器/镜像 |
| 8 | 启动新容器，设置自动重启 |
| 9 | 挂载data目录，持久化数据 |
| 10 | 验证服务状态，输出访问地址 |

### 6. 常用运维命令

```bash
# 查看容器日志
ssh root@49.235.161.106 "docker logs -f account-app"

# 重启容器
ssh root@49.235.161.106 "docker restart account-app"

# 停止容器
ssh root@49.235.161.106 "docker stop account-app"

# 查看容器状态
ssh root@49.235.161.106 "docker ps | grep account-app"
```

### 7. 注意事项
- 脚本会自动覆盖服务器上已有的jar包和Dockerfile
- 端口10011被占用时会自动关闭占用该端口的容器
- 容器已配置`--restart=always`，服务器重启后会自动启动
- data目录已挂载，CSV数据会持久化保存